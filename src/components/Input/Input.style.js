import { StyleSheet } from "react-native";

export default StyleSheet.create({
    container: {
        margin: 15,
        padding: 10,
        borderRadius: 10,
        borderWidth: 1,
        borderColor: 'purple',
        flexDirection: 'row'
    },
    input: {
        fontSize: 18,
        marginLeft: 10
    }
})